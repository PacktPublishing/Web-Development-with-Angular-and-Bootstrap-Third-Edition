import { ValidISBNDirective } from './valid-isbn.directive';

describe('ValidISBNDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidISBNDirective();
    expect(directive).toBeTruthy();
  });
});
