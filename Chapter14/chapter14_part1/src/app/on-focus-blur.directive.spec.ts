import { OnFocusBlurDirective } from './on-focus-blur.directive';

describe('OnFocusBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new OnFocusBlurDirective();
    expect(directive).toBeTruthy();
  });
});
