import { InputvalidationDirective } from './inputvalidation.directive';

describe('InputvalidationDirective', () => {
  it('should create an instance', () => {
    const directive = new InputvalidationDirective();
    expect(directive).toBeTruthy();
  });
});
